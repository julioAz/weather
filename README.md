# Wather Api using react

Create an app with https://home.openweathermap.org/ api to render the user weather using geolocation

## How to run this app

Just use the react workflow with npm/yarn in your machine.